name: CI

on:
  push:
    branches: [master]
  pull_request:

jobs:
  qemu:
    runs-on: ubuntu-latest

    steps:
      - name: QEMU - Cache
        id: cache-qemu
        uses: actions/cache@v4
        with:
          path: |
            /usr/local/bin/qemu-system-x86_64
            /usr/local/share/qemu
          key: qemu-10.1.2
          lookup-only: true

      - name: QEMU - Download and build
        if: steps.cache-qemu.outputs.cache-hit != 'true'
        run: |
          sudo rm -f /var/lib/man-db/auto-update && sudo apt update && sudo apt install -y libglib2.0-dev
          wget --no-verbose https://download.qemu.org/qemu-10.1.2.tar.xz
          tar -xf qemu-10.1.2.tar.xz
          cd qemu-10.1.2
          ./configure --target-list=x86_64-softmmu --static --without-default-features --enable-kvm
          make -j$(nproc)
          sudo make install

  test:
    needs: qemu
    strategy:
      matrix:
        version:
          - v5.15
          - v6.0
          - v6.6
          - v6.14

    name: Linux ${{matrix.version}}
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Linux - Restore cache
        id: restore-cache
        uses: actions/cache/restore@v4
        with:
          path: linux
          key: ${{matrix.version}}-${{hashFiles('linux/config')}}

      - name: Linux - Download
        if: steps.restore-cache.outputs.cache-hit != 'true'
        run: ./checkout_linux.py --version ${{matrix.version}} --tarball

      - name: Linux - Build
        if: steps.restore-cache.outputs.cache-hit != 'true'
        run: |
          sudo rm -f /var/lib/man-db/auto-update && sudo apt update && sudo apt install -y libelf-dev
          ./make_linux.py --reconfig

      - name: Linux - Save cache
        if: steps.restore-cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@v4
        with:
          path: linux
          key: ${{matrix.version}}-${{hashFiles('linux/config')}}

      - name: Build
        run: make

      - name: QEMU - Restore cache
        uses: actions/cache/restore@v4
        with:
          path: |
            /usr/local/bin/qemu-system-x86_64
            /usr/local/share/qemu
          key: qemu-10.1.2
          fail-on-cache-miss: true

      - name: Run QEMU
        timeout-minutes: 1
        run: ./run_qemu.py
