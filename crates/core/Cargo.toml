[package]
name = "kstep-core"
description = "kSTEP's shared host-side library: shared-memory decoder, QEMU command line, bug catalog, build and result conventions"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[features]
default = []
