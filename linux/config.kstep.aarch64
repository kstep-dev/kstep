# The kernel console is a PCI serial device (`-device pci-serial`) so that `console=ttyS0`
# holds on both architectures; kSTEP's own channels are virtio console ports.
CONFIG_SERIAL_8250_PCI=y
CONFIG_PCI_HOST_GENERIC=y
